<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_centerHorizontal="true"
    android:background="@drawable/background"
    android:weightSum="1">

    <ImageView
        android:id="@+id/iv_my_trip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        app:srcCompat="@drawable/my_trip" />

    <FrameLayout
        android:id="@+id/placeholder"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:layout_alignTop="@id/btn_list"
        android:layout_marginTop="50dp">
    </FrameLayout>

    <Button
        android:id="@+id/btn_map"
        android:layout_width="120dp"
        android:layout_height="40dp"
        android:layout_alignStart="@+id/iv_my_trip"
        android:layout_below="@+id/iv_my_trip"
        android:background="@drawable/button"
        android:text="@string/show_map"
        android:textColor="@android:color/background_light"
        android:textSize="18dp"
        android:textStyle="bold" />

    <Button
        android:id="@+id/btn_list"
        android:layout_width="120dp"
        android:layout_height="40dp"
        android:layout_alignEnd="@+id/iv_my_trip"
        android:layout_below="@+id/iv_my_trip"
        android:text="@string/show_list"
        android:background="@drawable/button"
        android:textColor="@android:color/background_light"
        android:textSize="18dp"
        android:textStyle="bold" />

    <Button
        android:id="@+id/btn_back"
        android:layout_width="90dp"
        android:layout_height="40dp"
        android:layout_centerHorizontal="true"
        android:layout_alignTop="@id/placeholder"
        android:layout_marginTop="310dp"
        android:text="@string/back"
        android:background="@drawable/button"
        android:textColor="@android:color/background_light"
        android:textSize="18dp"
        android:textStyle="bold" />

</RelativeLayout>